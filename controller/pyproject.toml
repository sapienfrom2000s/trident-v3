[project]
name = "trident-controller"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "kopf>=1.44.6",
    "kubernetes>=36.0.3",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
    "ruff>=0.16.6",
]

[tool.pytest.ini_options]
pythonpath = ["."]
