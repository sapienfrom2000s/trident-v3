[project]
name = "trident-backend"
version = "0.1.0"
description = "Trident v3 backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
]
